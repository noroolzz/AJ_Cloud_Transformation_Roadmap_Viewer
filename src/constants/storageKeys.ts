/**
 * LocalStorage keys for the Roadmap Viewer.
 * Aligned with Filters Panel blueprint (filter state persistence).
 */

export const STORAGE_KEYS = {
  FILTERS: 'roadmap_viewer_filters',
} as const;
